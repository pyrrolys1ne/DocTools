# Desktop UI Decision

The wide-workspace desktop layout (prototype A) was selected. It keeps grouped operations in a stable left navigation and uses the remaining window width as one rounded work surface, avoiding a narrow form and unused space on the right.

The production layout uses a cool gray-blue application shell, a neutral surface, softly rounded controls, and vertically stacked source/output path fields.
